# Contributing

Keep each change scoped to one roadmap task. Run restore, Release build, tests, and format verification before opening a review.

Public contracts require XML documentation, nullable reference types remain enabled, and warnings are treated as errors. Keep business logic out of WPF views and do not access EF Core directly from ViewModels.
